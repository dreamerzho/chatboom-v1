前端重构与优化执行计划 V3 (Actionable Plan)
文档目标: 本文档旨在将前期的战略复盘和UI/UX评估,结合新提出的核心功能需求,转化
为一个可执行的前端开发任务列表。核心目标是使前端应用的功能、设计和体验与《广告公
司服务监测软件 - 产品需求与开发规划文档 V1.0》的核心愿景保持高度一致。
执行原则: 任务按优先级(P0 > P1 > P2 > P3)顺序执行。每个任务应在完成后进行代码审查
,确保其满足验收标准。
Phase 0: 基础架构与方向统一 (P0 - 最高优先级)
目标: 在开始具体功能开发前,建立稳固的、统一的页面布局和术语规范。
● 任务 0.1: 确立全局页面布局 (Establish Global Page Layout)
○ 背景: 采纳新版设计中清晰的“左侧导航 + 右侧内容”的经典布局。
○ 操作:
1. 实现主框架: 构建一个全局的、二分栏的页面布局结构。
2. 左侧边栏 (Sidebar): 包含指向核心模块的图标和文本链接: 数据概览, 员工视
图, 项目视图, 文件视图, 系统设置。当前选中的导航项需有明确的视觉高亮状
态。
3. 右侧内容区 (Main Content): 作为所有具体页面内容的渲染区域。
○ 验收标准: 应用已具备稳定的左-右布局结构。
● 任务 0.2: 全局术语统一 (Terminology Alignment)
○ 操作: 在整个前端代码库中,将所有面向用户的界面文本与产品需求文档的命名对
齐。
○ 验收标准: 应用中不再出现旧的术语。
Phase 1: 核心价值交付与分析功能实现 (P1 - 高优先级)
目标: 在新的布局框架内,快速上线能够体现产品核心分析价值的模块,并修正基础数据模
型。
● 任务 1.1: 重构【仪表盘】内容区 (Refactor Dashboard Content Area)
○ 操作: 在右侧内容区,移除静态KPI卡片,替换为动态分析组件。
○ 要新增的组件:
1. “工作量最高的员工排行榜”
2. “近期负面反馈”
3. “近期动态”
○ 验收标准: 仪表盘内容区成功展示了至少两个新的动态分析组件。
● 任务 1.2: 实现【系统设置】核心功能 (Implement Core Se
ings)

○ 操作: 开发系统设置页面中的核心管理功能。
○ 具体功能点:
1. 员工映射管理

2. 未映射用户列表
○ 验收标准: 管理员可以在设置页面中顺畅地增、删、改、查员工映射关系。
● 任务 1.3 (新增): 区分员工与客户角色 (Di

erentiate Employee and Client Roles)
○ 背景: 解决当前“员工管理”中混入甲方客户,导致数据分析不准确的问题。
○ 操作:
1. 改造员工映射功能: 在“系统设置”的“员工映射管理”模块(任务1.2)中,为每个
用户(user_mapping)增加一个 “角色” (Role) 字段。
2. 实现UI: 该字段应为一个下拉选择框,至少包含两个选项: “内部员工” 和 “外
部客户”。
3. 数据过滤:
■ 【员工视图】 页面未来只应展示“角色”为“内部员工”的用户。
■ 工作量分析等核心功能,应以“内部员工”的数据为统计口径。
○ 验收标准: 可以在系统中明确区分内部员工和外部客户,且“员工视图”只展示内部
员工。
Phase 2: 视图增强与业务逻辑深度集成 (P2 - 中优先级)
目标: 将公司特定的业务逻辑(特别是文件命名规范)和核心工作流(如数据同步)深度集成到
UI中。
● 任务 2.1: 重构【项目视图】与项目设置 (Redesign Project View & Settings)
○ 操作: 将“项目视图”从简单的列表,升级为包含完整管理闭环的功能模块。
○ 2.1.1 列表项改造 (List Item Redesign):
■ 信息: 必须包含项目名称、总文件数、负面关键词总数。
■ 健康指示器: 根据负面关键词数量,增加一个视觉指示灯(红/黄/绿)。
■ 交互: 项目卡片应直观具备以下按钮:
1. 报告 (Report):
■ 点击功能: 弹出一个报告生成选项的模态框,用户可选择报
告类型(如:项目概览报告、负面关键词趋势报告等)和时间
范围,点击生成后可下载PDF或Excel格式的报告。

2. 查看详情 (View Details):
■ 点击功能: 跳转至该项目的专属详情页面,展示更全面的项
目信息、关联文件列表、历史活动记录、负面关键词分析图
表等。
3. 编辑 (Edit):
■ 点击功能: 弹出一个编辑模态框,允许修改项目名称、项目
描述、关联群聊等信息。
4. 同步数据 (Sync Data):
■ 点击功能: 立即触发该项目关联群聊的数据同步操作,并显
示同步进度或结果提示。如果已有数据同步功能,此按钮应
调用该功能。
5. 删除 (Delete):

■ 点击功能: 弹出确认删除的提示框,确认后删除该项目及其
所有关联数据(需谨慎操作,可能需要管理员权限)。
○ 2.1.2 (新增) 强化项目编辑与数据同步功能 (Enhance Project Editing & Data
Sync):
■ 改造编辑弹窗: 点击“编辑”按钮后,弹出的窗口应包含以下可配置项:
1. 项目名称 (文本输入)
2. 项目描述 (文本域)
3. 关联群聊: 一个多选框或标签输入框,允许将一个或多个已被系统
监控的微信群聊关联到此项目。

■ 实现数据同步功能: 在编辑弹窗内或项目详情页中,增加“数据同步”模块,
提供至少两种同步方式:
1. 自动同步: 提供一个日期范围选择器。用户选择起止时间后,点击
“同步”按钮,前端将请求后端,拉取关联群聊在该时间段内的所有
聊天记录。
2. 手动上传: 提供一个文件上传区域,允许用户直接上传从微信等工
具导出的聊天记录文件(如 .json 或 .txt 格式),并将其归档到该项
目下。

○ 验收标准: 项目视图的列表能直观反映项目风险,并提供快速操作按钮。用户可以
方便地为项目关联数据源(群聊),并按需发起数据同步,并能便捷地查看项目详情
和生成报告。

-----
● 任务 2.2 (升级): 设计与实现数字资产库 (Design & Implement Digital Asset
Library)
○ 背景: 将原有的“文件视图”升级为功能更全面的“数字资产管理”中心,解决核心资
产管理缺失的问题。
○ 操作:
1. 提供双重视图模式:
■ 列表视图 (List View): 适用于技术性查阅,展示文件名、作者、版本、项目
等详细信息,并支持按列排序。
■ 网格视图 (Grid View): 适用于创意类查阅,以缩略图形式展示图片和视频
,并为文档等其他类型文件提供统一的类型图标。
2. 增强文件预览功能:
■ 点击图片或视频的缩略图,应在弹出的模态框(Lightbox)中直接预览,无
需下载。
■ 点击文档类文件,应在新标签页中打开或尝试在模态框中嵌入预览。
3. 构建资产详情页:
■ 点击任一资产,可进入一个独立的详情页面。
■ 此页面应展示高清预览、所有解析出的元数据(来自文件名)、以及明确的
“下载源文件”按钮。
■ (远期目标) 展示与该文件相关的上下文信息,如“关联的聊天记录片段”。

4. 强化搜索与筛选: 除了基于元数据(作者、版本)的筛选器,在页面顶部提供一
个全局搜索框,支持按文件名进行模糊搜索。
○ 验收标准: 用户拥有一个功能完善的数字资产库,可以方便地通过两种视图模式浏
览、预览、搜索和下载公司的所有项目文件。
Phase 3: UI/UX 体验打磨 (P3 - 低优先级)
目标: 优化应用中的交互细节,提升整体专业度和用户满意度。
● 任务 3.1: 优化空状态文案 (Enhance Empty States)
○ 操作: 审查所有可能出现空状态的页面,将“暂无数据”替换为更有帮助的引导性文
案。
○ 验收标准: 应用中所有的空状态都提供了上下文相关的、有帮助的提示信息。
● 任务 3.2: 升级【员工视图】为分析视图 (Evolve Employee View)
○ 操作: 在 任务1.3 的基础上,将“员工视图”的表格中增加“近30天提交文件数”、“参
与项目数”等统计列。
○ 验收标准: 员工视图提供了初步的工作量化数据。